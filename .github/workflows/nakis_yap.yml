name: Janome MB4 Logo Nakis

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Kodlari Cek
        uses: actions/checkout@v4

      - name: 🐍 Python Kur
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: 📦 Kutuphaneleri Kur
        run: |
          python -m pip install --upgrade pip
          pip install pyembroidery pillow

      - name: 🧵 Resimden Nakis Uret
        run: |
          python create_pivaz.py

      - name: 📤 Nakis Dosyalarini Yukle
        uses: actions/upload-artifact@v4
        with:
          name: JANOME_MB4_LOGO_FINAL
          path: |
            tatli_duragi.dst
            tatli_duragi.jef
